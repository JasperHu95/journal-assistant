## V1 开发要求
该程序用于追踪学术期刊的发文，以帮助研究者追踪感兴趣的期刊，以免错过期刊发文。
V1版本有如下要求：
1. 程序尽可能精简，拥有极快的启动速度和低内存占用；
2. 拥有现代美观的UI界面，在现代美观的基础上，也应具备学术期刊的严肃复古特性，可以参考Quartly Journal Of Economics的那种形象风格；
3. 在开发时就考虑多系统的部署问题，如win11，linux，macos，但V1版本先以win11为基础，且这应是64位的程序；
4. 使用RSS作为订阅来追踪，保证合法性和可靠性，但是应支持两种方式：（1）直接输入RSS源；（2）输入网址让程序自行寻找RSS订阅链接；
5. 拥有清晰医用的RSS源管理方式；
6. 对于订阅而产生的消息，应有未读和已读标识，来帮助使用者明确信息的摄取；
7. 拥有tag功能，以便使用者对信息作出分类，tag由使用者自行编辑，并且需要有一个tag管理工具来帮助使用者管理增加或删除tag；
8. 支持中英双语，并且附带切换按钮；
9. 需要有exe文件，以便win11系统运行。
10. 整个开发过程不允许使用颜表情和emoji

开发标准：
A. 作为一名专业开发者，应保持代码的清晰，简洁并附有相关注释（不需要详细注释，但应简洁并阐明代码）
B. 开发过程中应保持反思，分阶段回顾并完善代码
C. 在写完代码后，应审查代码2遍，并修正代码。
D. 在完成开发后，回到该文件，简述本次开发过程，以便日后复盘，对于在本文件中记录的内容，应保持简洁明了，并遵循markdown语法。

---

## V1 开发日志 (2026-07-18)

### 技术栈

| 层 | 选择 |
|---|---|
| 桌面框架 | Tauri v2 (Rust) |
| 前端 | Svelte 5 + TypeScript + Vite |
| 样式 | Tailwind CSS 4 |
| 数据库 | SQLite (tauri-plugin-sql, 前端 JS API) |
| RSS | feed-rs + reqwest + scraper |

### 架构决策

- 数据库操作全部在前端通过 `@tauri-apps/plugin-sql` 完成，Rust 后端仅负责 RSS 抓取/解析/发现等业务逻辑
- 前端不使用 SvelteKit，采用 vanilla Svelte + 手动路由（更轻量，适合桌面应用）
- UI 采用 QJE 学术复古风格：酒红(#8B1A2B) + 暖奶油纸色(#FAF7F2) + Cormorant Garamond 字体

### 开发过程

1. **Phase 0**: 搭建 Tauri + Svelte 项目骨架，配置 Tailwind CSS 和 SQLite 插件
2. **Phase 1**: 实现 Rust RSS 引擎（parser/discovery/fetcher）和前端数据库层（db.ts）
3. **Phase 2-4**: 实现 RSS 源管理、文章列表（含已读/未读）、标签管理的完整 UI
4. **Phase 5**: 中英双语系统（i18n.ts + localStorage 持久化）
5. **Phase 6**: QJE 学术复古风格打磨（配色、字体、间距、滚动条）
6. **Phase 7**: Linux 平台构建验证（release binary 25MB）
7. **Phase 8**: 两轮代码审查 - 修复 clippy 警告，确认无 unwrap/todo/unsafe/emoji

### 遇到的问题

- Bazzite 系统 npm 默认 `omit=dev`，导致 devDependencies 不安装，通过 `.npmrc` 覆盖
- tauri-plugin-sql v2 不在 Rust 侧暴露 SqlitePool，需将数据库层移至前端
- rpm-ostree 安装系统依赖后需要重启才能生效

### 待办 (V2 方向)

- Win11 交叉编译打包 (.exe)
- 系统通知（新文章到达）
- OPML 导入/导出
- 暗色模式
- 文章收藏/星标
- 自动刷新间隔设置


---
## V2 开发要求
V2是在V1的基础上进行的改进，这是开发的基础。

1. 在V1版本中，文章并没有按照期刊被分类。因此，我希望在V2中增加一个归类功能，使得RSS订阅源能够按照期刊被整理。
2. 在V1版本中，订阅的内容不够全面，比如订阅过来的论文只有标题而没有其他内容。同时，我发现，V1版本能够通过按键去到论文的网页。因此，我希望在V2版本中增加一个功能，使得订阅器能够通过链接至少摘取论文的摘要。
3. 在V1版本中，订阅器并没有附带翻译功能，这可能对不熟悉英文的使用者造成影响。因此，我希望在V2版本中加入AI翻译功能。这个AI翻译功能仅仅用于翻译文章内容，而不是用于翻译UI界面的功能。为了程序足够精简，我希望使用API方式的来进行翻译，比如接入DeepSeek的API进行翻译，这样程序就不需要内置一个庞大的翻译系统。对于这个功能，我希望在UI界面中增加一个单独的按钮，供用户来接入API。这一版我希望仅接入Deepseek的API，你应该将前期配置完成，确保用户只需要输入API即可。

### V2 开发日志 (2026-07-19)

#### 开发模式
- **Kimi Code (K3)**：主要开发者，负责编写代码
- **Claude Code (DeepSeek V4 Pro)**：监督者，负责代码审查
- **Hermes Agent**：总指挥，负责调度、验证、构建

#### 新增功能

1. **期刊归类**：三栏布局（期刊列表 | 文章列表 | 详情），按 feed 分组展示文章
2. **摘要提取**：从论文 URL 抓取 `<meta name="description">` 或正文首段，含 SSRF 防护
3. **AI 翻译**：接入 DeepSeek API，设置页面配置 Key，文章详情页一键翻译

#### 开发过程

1. Kimi 开发期刊归类 → Claude 审查发现 5 个问题 → Kimi 修复
2. Kimi 开发摘要提取 → Claude 审查发现 2 个中危（SSRF + 错误反馈）→ Kimi 修复
3. Kimi 开发翻译功能 → Claude 审查发现 ArticlesPage 缺翻译 UI + 竞态条件 → Kimi 修复

#### 技术改进
- fetcher.rs: 浏览器 UA、超时、GBK/GB2312 编码自动检测
- parser.rs: 链接优先 alternate/guid fallback、多作者连接、categories 提取
- discovery.rs: 扩展 MIME 类型、reqwest::Url 解析、去重
- 新增 extract.rs 模块：从网页提取摘要
- 新增 settings 表：存储 API Key
- 新增 translate_text 命令：调用 DeepSeek API 翻译

#### 遇到的问题
- Kimi Code 执行长任务容易超时（600s 限制），需要拆分任务
- Claude 审查质量高，多次发现实际 bug（竞态条件、状态锁死、UI 缺失）


##　V3 开发要求
V3是在V2的基础上进行的改进开发，这是本次开发的基础设定。
进入到V3后，我们的开发工作将进一步细化，会使用例如V3.1 3.2 3.3这样的方式推进开发。

### V3.1 功能要求
1. 在V2版本中提到的摘要获取功能并没有被实现，请在V3.1中实现。
2. 在项目文件夹后，有一个RSS_List.md文件，这里记录了我找到的一些经管顶尖期刊的RSS源，请注意，并不是每一本期刊都设置了RSS，所以会存在缺失的情况。对于每个RSS源，都被验证过。我希望你设置一个单独的功能模块，让用户可以选择订阅现有期刊中的哪些。同时，你也应设定取消订阅的选项，以方便用户调整。
3. 请删除翻译功能，我认为暂时没有必要了。
4. 对于订阅来的信息，我经常能看到`<p>`这样的情况，我不知道是为什么，请修复这类情况。总而言之，这一版本中，你需要尽可能优化用户的阅读体验，这是至关重要的事情。